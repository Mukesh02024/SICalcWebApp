@model IEnumerable<SICalcWebApp.Areas.RiceMill.Models.MillBunker>

@{
    ViewData["Title"] = "ListMillBunekr";
}

<h1>ListMillBunekr</h1>

<p>
    <a asp-action="AddMillBunekr">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MillBName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MillBName)
            </td>
            <td>
                    <a asp-action="EditMillBunekr" asp-route-id="@item.Id" class="btn btn-primary">Edit</a> |



                    <form asp-action="DeleteMillBunekr" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="@item.Id" />
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this item?');">
                            Delete
                        </button>
                    </form>
         
            
            </td>
        </tr>
}
    </tbody>
</table>
